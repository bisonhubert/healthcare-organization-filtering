<% provide(:title, "Resource Search") %>

<header class="site_header">
  <h1 class="page_heading"><%= yield(:title) %></h1>
</header>

<main>
  <section class="eligibility_filters">
    <%= render "eligibility_filters" %>
  </section>

  <section class="organizations">
    <ul class="organizations_list">
      <%= render @organizations %>
    </ul>
  </section>
</main>